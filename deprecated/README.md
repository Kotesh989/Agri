# Deprecated Reference Files

This folder contains legacy Prisma/PostgreSQL files kept only for reference.

The active backend uses MongoDB with Mongoose models in `backend/src/models/index.js`.

Do not use these files for local setup, migrations, Render deployment, or production configuration.
